# My vim and ideavim settings

To change location of .ideavimrc file use `:source <path>` command or create symlink to new location.

To change location of .vimrc use `vim -u <path>` or create symlink to new location.

